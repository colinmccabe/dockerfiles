#!/bin/sh

set -eu

x11docker --xpra-xwayland --no-auth --user=RETAIN \
  --name=vlc \
  --pulseaudio=socket \
  -- \
  --cap-drop=ALL --security-opt=no-new-privileges \
  -v "$(pwd)":/home/appuser/workdir:ro \
  --device=/dev/dri \
  --device=/dev/sr0 \
  -- \
  vlc "$@"
