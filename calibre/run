#!/bin/sh

set -eu

docker run --rm -it \
  --cap-drop=ALL --security-opt=no-new-privileges \
  --read-only --network=none \
  -u 801:801 \
  --tmpfs=/tmp \
  --tmpfs=/home/appuser/.config/calibre:uid=801,gid=801 \
  --mount type=bind,src="$(pwd)",dst=/home/appuser/workdir \
  calibre \
  "$@"
